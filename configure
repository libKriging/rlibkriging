#!/usr/bin/env sh

. tools/cmake_config.sh
if test -z "${CMAKE_BIN}"; then
   ## also error to end configure here
   as_fn_error $? "Could not find 'cmake'." "$LINENO" 5
fi

. tools/r_config.sh

echo `$CMAKE_Fortran_COMPILER --help`

. tools/build.sh